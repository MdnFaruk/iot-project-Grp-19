# Thermal Comfort Prediction — IoT Project (Group 19)

### Team
- Md Nuruzzaman Faruk
- Naganandhini Somalinga Mohanlal
- Muhammad Faizan Tanveer
- Arshman Tariq
- Jamiual Topu

---

## 📋 Overview
Simple student project that monitors temperature, pressure, humidity and shows a comfort state. The demo runs locally with `main.py`. There's an optional ML script `ml_main.py` that can train a simple model (using `machine_learning/training_data.csv`) and run inference.

## Files 💾
- `main.py` — simple demo (no extra deps)
- `ml_main.py` — optional ML pipeline: trains/loads a model and runs inference
- `config.py` — basic settings
- `machine_learning/` — optional helpers and training data

## ▶️ How to run
Run the basic demo:
```bash
python main.py
```
Run the ML pipeline (optional):
```bash
python ml_main.py
```



